# S03：预算、原子投影与资源约束实现证据

- 日期：2026-09-16
- 状态：S03 首轮父审计 A01–A03 已修复并完成本分支复验，等待父任务复审；S04–S10 未实施
- 仓库：`LeXwDeX/OpenCode-GraphAgent`
- 工作树：`/Users/suntao/.codex/worktrees/bf92/opencode-dag`
- 分支：`feat/native-context-folding-s03`
- 起点：经父任务审计通过的 S02 提交 `ac3b5c92e0c7a727eccad9a313f94e5ec1677b65`
- 规格 Issue：[GitHub #605](https://github.com/LeXwDeX/OpenCode-GraphAgent/issues/605)
- 既有 Draft PR：[GitHub #606](https://github.com/LeXwDeX/OpenCode-GraphAgent/pull/606)，其 source 仍为 S01 分支；本子任务未修改或另建 PR

## 1. 范围与结论

本步只扩展共享 `packages/core/src/session/context-folding/`：实现准备请求预算、最终 wire 映射校验、收益选择、固定占位和独立副本原子投影，并在 S02 规范化/扫描路径内加入有界工作预算。没有修改 OpenCode 或 Core runner 的产品发送路径，没有新增配置、身份台账、provider ID 变换、模型调用或跨请求内容缓存；具体宿主消息提取与替换仍属于 S05/S06。因此本步没有可部署产品行为，也不把共享层测试通过描述为功能已经默认启用。

新增文件：

- `packages/core/src/session/context-folding/budget.ts`
- `packages/core/src/session/context-folding/projection.ts`
- `packages/core/src/session/context-folding/wire-value.ts`
- `packages/core/src/session/context-folding/work-budget.ts`
- `packages/core/test/session/context-folding-projection.test.ts`

修改 S02 的 `normalize.ts`、`plan.ts`、`policy.ts`、`types.ts`、`index.ts`，并更新本实施计划。没有修改 S02 的候选资格、严格身份、近期保护或完整正文相等规则。

首轮父审计报告 `docs/context-folding/audits/S03-review-01.md` 的三项 P1 已在原 S03 分支修订：

- A01：共享指纹函数现在显式接收并分别严格序列化实际 `request`、当前 model/provider/runtime identity 和完整预算输入；投影函数自行以 `input.request` 重算，不能用旁路 identity 中的旧 request 替代。call/result 映射新增实际 wire ID 路径，每个声明 ID 都从最终 request 读回核对。
- A02：wire clone 对循环或任意共享对象别名统一失败关闭，不再在私有副本保留别名。写入后使用两个独立工作预算逐字段比较原 request 与投影副本，只允许选中的正文路径发生声明的字符串替换。
- A03：普通及 null-prototype 对象用 `Object.defineProperty` 创建安全自有数据属性，避免 `__proto__` setter；保留 `__proto__`、`constructor`、`prototype`、枚举性及对象原型。

父审计三例的独立修订探针 `/private/tmp/context-folding-s03-parent-probe-fixed.ts` 使用 Bun 1.3.14：旧 request ID 返回 `stale-request`，source 到非选中路径的别名返回 `projection-failed` 且原文不变，合法 `__proto__` 对象成功投影并保持全部自有字段与原型。

## 2. 预算与选择契约

- 预算使用 `U = min(I ?? C, C - O)`、`T = floor(U * 0.7)`；仅 `E > T` 继续。`C`、显式 `I`、最终 `O` 和协议开销必须是有效安全整数，未知输出预留不按零处理。
- `E` 对实际传输的 messages、wire-visible tool 名称/描述/schema、system 所在位置和协议开销做一次共享 `Token.estimate`。system 契约只能选择 `messages`、`instructions`、`none` 或 `unknown`；进入 messages 时不再传第二份 system 值，instructions 则明确单独计量。可执行 tool closure、未知媒体、访问器、异常对象或不可序列化 schema 使整次请求跳过。
- 候选按 S02 的旧到新顺序选择。正文与固定占位的 JSON 字符串估算差额为单项净收益，`511` 不选、`512` 和 `513` 选择；累计估算达到 `T` 即停止。候选耗尽仍超目标只记录 `overBudget: true`，不删除唯一内容，也不触发摘要或重试。
- 工具 schema、消息、system 位置、模型/runtime identity 或最终输出预留都由本次准备输入提供；共享层没有上次请求的预算或模型缓存。

## 3. 最终 wire 映射与原子投影

两个后续适配器复用 `WireProjectionSnapshot` 契约：分别列出最终 provider 可见的 call 与 result，对每项提供稳定 source ref、最终可见 call ID、该 ID 在实际 request 中的路径、顺序和唯一正文路径。共享层要求：

1. call/result source ref 和最终 call ID 均一一对应且全局唯一；每个 ID 从实际 request 的声明路径读回核对，碰撞、旁路表与 wire 不一致、重复 ref、缺失 result 或重复路径整次回退。
2. source/witness 均完整存在于本次最终请求，witness 顺序更晚、未成为任何 source，且最终 wire 正文逐字相等。
3. 共享层用实际 request、当前 identity 和完整预算分别生成分段指纹，再合成请求指纹；它必须同时匹配计划捕获值和映射捕获值。正文、输入、外层 metadata、最终 ID、模型、runtime、system/messages/tools、限额或适配器 identity 任一变化都会使旧计划失效。
4. witness call ID 先限制为最多 128 UTF-8 bytes，再用 JSON 字符串规则转义后放入唯一固定英文占位；过长或空引用跳过，不解释为指令。
5. 投影先严格深拷贝整个可序列化 request root；发现循环或共享对象别名直接拒绝，再只修改已验证正文路径。所有替换完成后有界遍历整棵原 request 和投影副本，只有声明的 source 字符串可变化；任何 clone、路径、写入、原型/自有键/非选中字段、共享别名或复核失败都丢弃私有副本并返回同一个原请求对象。

共享层没有 OpenCode AI SDK、Native LLM 或 Core `TextPage` 的具体结构分支；S05/S06 必须从各自最终 wire 生成上述映射，并完成真实宿主正向测试。

## 4. 资源保护与固定上限

资源计数在候选规范化和身份字符串构造之前开始，并在递归扫描、JSON 线化、深拷贝与哈希分桶过程中持续执行。数组在按长度构造任何索引集合之前检查；规范化深度在递归进入时检查；字符串先逐 code unit 计算 UTF-8 bytes，确认预算后才构造规范化或 wire 字符串。超限统一精确跳过为 `work-limit`，不截断、抽样或近似比较。

固定内部上限：

| 维度                               | 上限                       | 行为                                    |
| ---------------------------------- | -------------------------- | --------------------------------------- |
| 每阶段 UTF-8 输入                  | 8 MiB                      | 超限立即 `work-limit`                   |
| 中间字符串累计字符预算             | 64 MiB                     | 防止规范化/JSON 父级拼接放大            |
| steps / candidates / wire mappings | 4,096 / 4,096 / 4,096 each | 在候选字段扫描、映射索引和规范化前检查  |
| 单 fingerprint bucket              | 1,024 entries              | 第 1,025 项跳过整次计划，不降级近似相等 |
| nodes / container items            | 131,072 / 131,072          | 覆盖对象、数组、预算序列化和独立副本    |
| 递归深度                           | 64                         | 进入第 65 层前失败关闭                  |

这些上限是首版内部常量，不是公开配置。输入字节、候选数和强制碰撞桶已实际基准；深度、节点及容器上限通过退化测试验证 fail closed，但本步没有把它们包装成跨硬件性能保证。

## 5. 测试覆盖

新增测试文件 20 项；与 S02 的 21 项合计 41 项、7,304 个断言。覆盖：

- U15：超预算但只有唯一结果时原请求不变且保留 `overBudget`。
- U16/U17：净收益 511/512/513，软目标相等/超过，按旧到新选择、达到目标停止及候选耗尽仍超额。
- U19/U20：witness 缺失、截断、重排、正文变化、重复最终 call ID、identity 失效、路径错误和中途投影失败全部原子回退。
- U21/U22：冻结输入可运行、重复投影不形成引用链，原请求不被改写。
- I04/I05/I06：system 的 messages/instructions 位置、当前 messages/tools schema 重估、模型 identity 与最终映射变化使旧计划失效。
- I11：引号、反斜线和换行引用被固定转义；过长和碰撞引用不插入。
- A01：actual request 的最终 ID、输入、metadata，以及 model、tools、输出预算独立变化全部失效；即使重新捕获指纹，旁路 ID 与实际 wire 不一致仍拒绝。
- A02：source 与独立 user/metadata/未选中 result、source/witness 或多个 source 共享对象均原子拒绝；成功路径逐字段复核所有非选中内容。
- A03：`__proto__`、`constructor`、`prototype` 和 null-prototype 对象投影后自有字段、JSON 表示与原型不变。
- 深层、稀疏、超大 ASCII/多字节输入、候选上限和退化哈希桶均固定 `work-limit`。

## 6. 实际基准

硬件与运行时：Apple M4 Pro Mac mini（14 核，64 GB，arm64），Darwin 27.0.0，Bun `1.3.14 (0d9b296a)`。敏感设备标识未记录。每个字节规模在独立进程中先 warm-up 2 次；64 KiB–1 MiB 运行 12 次，2–4 MiB 运行 5 次。数据是两个完全相同 ASCII 正文的 synthetic request；表中“正文总量”是两个正文的 UTF-8 bytes，峰值 RSS 来自 macOS `/usr/bin/time -l`。

| 单正文  | 正文总量 | planner p95 | 完整预算+映射+投影 p95 | 进程峰值 RSS | 结果                                 |
| ------- | -------- | ----------- | ---------------------- | ------------ | ------------------------------------ |
| 64 KiB  | 128 KiB  | 0.242 ms    | 1.730 ms               | 93.3 MB      | 1 replacement                        |
| 256 KiB | 512 KiB  | 1.222 ms    | 5.304 ms               | 105.6 MB     | 1 replacement                        |
| 1 MiB   | 2 MiB    | 3.247 ms    | 20.023 ms              | 130.7 MB     | 1 replacement                        |
| 2 MiB   | 4 MiB    | 6.657 ms    | 42.497 ms              | 134.0 MB     | 1 replacement                        |
| 3 MiB   | 6 MiB    | 10.030 ms   | 62.472 ms              | 175.9 MB     | 1 replacement                        |
| 4 MiB   | 8 MiB+   | 3.953 ms    | 未进入投影             | 62.0 MB      | request metadata 使指纹 `work-limit` |

候选/碰撞轴：唯一 fingerprint 的 4,096 candidates 为 p95 8.969 ms、峰值 RSS 149.0 MB；4,097 在扫描前返回 `work-limit`。强制所有 fingerprint 相同的 1,024 candidates 为 p95 2.365 ms、峰值 RSS 119.5 MB；第 1,025 项固定 `work-limit`。完整原始数据见：

- `/private/tmp/opencode-dag-s03-hardware.log`
- `/private/tmp/opencode-dag-s03-benchmark.log`
- `/private/tmp/opencode-dag-s03-candidate-benchmark.log`
- `/private/tmp/opencode-dag-s03-collision-benchmark.log`
- `/private/tmp/opencode-dag-s03-fix-benchmark.log`
- benchmark harness：`/private/tmp/context-folding-s03-benchmark.ts`、`/private/tmp/context-folding-s03-collision-benchmark.ts`

限制：synthetic ASCII 数据不代表真实模型质量；RSS 含 Bun/JIT/进程基线，样本量较小且 GC 使 RSS 不单调；基准没有网络、provider 序列化或宿主 adapter。首轮审计修订增加实际 request/budget 分段指纹和完整非选中字段校验，因此 3 MiB 单正文的完整路径 p95 从首版 36.343 ms 增至 62.472 ms，峰值 RSS 从 143.4 MB 增至 175.9 MB；仍受既有字节、节点、深度和容器上限约束。该基准只支持当前工作量上限与 fail-closed 行为，不证明费用下降、缓存收益或所有硬件上的相同延迟。

## 7. 实际门禁

所有产品命令使用仓库锁定的 `bun@1.3.14`。依赖以 `bun install --frozen-lockfile --ignore-scripts` 恢复，并单独初始化 Husky；这不表示本机 Xcode license 阻塞的原生 lifecycle 构建已经通过。

| 检查                          | 结果                                               | 原始结果路径                                              |
| ----------------------------- | -------------------------------------------------- | --------------------------------------------------------- |
| S02+S03 定向测试              | 41 pass、0 fail、7,304 assertions                  | `/private/tmp/opencode-dag-s03-fix-targeted-test.log`     |
| 新增范围定向 oxlint           | 0 warnings、0 errors                               | `/private/tmp/opencode-dag-s03-fix-targeted-lint.log`     |
| 父审计三例独立修订探针        | A01/A02/A03 均符合失败关闭或安全投影预期           | `/private/tmp/opencode-dag-s03-parent-probe-fixed.log`    |
| Core 完整测试                 | 1,274 pass、0 fail、11,058 assertions              | `/private/tmp/opencode-dag-s03-fix-core-test.log`         |
| 根 typecheck                  | 29/29 tasks successful                             | `/private/tmp/opencode-dag-s03-fix-root-typecheck.log`    |
| 根 lint                       | 0 errors；4,844 个既有 warning，低于 4,850 ratchet | `/private/tmp/opencode-dag-s03-fix-root-lint.log`         |
| Prettier / `git diff --check` | 通过                                               | `/private/tmp/opencode-dag-s03-fix-format-diff-check.log` |

## 8. 图索引与交付边界

当前 worktree 已以 full 模式刷新项目 `Users-suntao-.codex-worktrees-bf92-opencode-dag`；generation `2026-09-16T03:53:46Z`，状态 ready，64,303 nodes / 235,732 edges，branch 为 `feat/native-context-folding-s03`。`projectContextFoldingRequest` 的最终 inbound trace 在排除测试时为 0 callers，包含测试时只有新增测试文件；直接 `rg` 也只找到共享层导出、实现和测试，证明本步没有接入产品发送路径。outbound trace 对应预算、映射校验、wire clone/path、共享 token/hash 与工作预算函数。

全部 S03 源码、两份测试、两份文档及复用的 `util/token.ts` / `util/hash.ts` 均为 `metadata_match` 且 `no_recorded_issue`；`packages/core/src/session/context-folding` scope 也无记录缺口。仓库其他目录仍有 70 个已披露的 partial parse 文件，但不包含上述范围。coverage clean 只表示无记录缺口，不替代本步的直接源码读取、字面量搜索和真实测试。实施前 trace 曾出现低置信度误连 `packages/opencode/src/plugin/meta.fingerprint`；直接源码确认真实依赖是注入函数或 `Hash.sha256`，未采用该错误边。

实际 `specgit 2.0.1` 的 `init --check` 和 Issue #605 inspect 成功；`specgit pr --status --request 606` 因当前 S03 source 与 PR #606 的 S01 source identity 不同而返回 exit 3 `identity_mismatch`，符合预期。本子任务没有改写、另建、ready、合并或发布 PR；分支 push 后由父任务独立审计，再决定聚合。
