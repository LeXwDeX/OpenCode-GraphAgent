# S06：Core runner 原生上下文折叠接线证据

- 日期：2026-09-16
- 状态：S06 父审计 A01/P1 修复已完成当前分支复验，等待父任务重新独立审计；S07–S10 未实施
- 仓库：`LeXwDeX/OpenCode-GraphAgent`
- 工作树：`/Users/suntao/.codex/worktrees/cd4e/opencode-dag`
- 分支：`feat/native-context-folding-s06`
- 起点：S05 审计修复提交 `f7ac15155b1e5819548baf5fe0255b8054e463b2`
- 规格 Issue：[GitHub #605](https://github.com/LeXwDeX/OpenCode-GraphAgent/issues/605)
- 既有 Draft PR：[GitHub #606](https://github.com/LeXwDeX/OpenCode-GraphAgent/pull/606)，其 source 仍为 S01 分支；本子任务未修改或另建 PR

## 1. 范围与结论

本步把 S02/S03 的共享规划、预算和原子投影接入 Core 普通 conversation runner。投影发生在完整 `LLM.Request` 建立后、现有 `compactIfNeeded` 前；摘要仍读取原始 entries，最终发送只使用独立投影请求，不回写会话存储、工具结果或下一轮历史。

Core 新增有界、实例内存型执行来源台账。实际 application 注册和普通 location/local 注册均记录为 `custom`；只有通过 host-internal capability 注册的实际随程序发布的 `read`、`grep`、`glob` 才记录为 `host-builtin`。公开 `Tool.make({ contextFolding: ... })` 标记只描述输出契约，不授予来源身份；普通工具即使同名 shadow 内置工具并声明 `instructions: "none"` 也仍失败关闭。每次 materialize 激活有效注册集合及递增 revision 的 generation，只有该集合中实际成功结算的调用才入账；安装并移除 override 后，即使恢复到相同注册签名，override 前捕获的旧 turn 仍不能写入新 generation。同名 override、缺失记录、generation 变化和新实例重启前历史均不能仅凭工具名放行。台账不保存参数、正文、路径、指纹、计划或跨请求内容缓存。

内置 `read` 只接受完整且字段精确的 `ReadTool.TextPage`：正文为 `content`，`type/mime/offset/truncated/next`、完整输入、metadata 及持久化外层字段均绑定。投影只替换最终 provider wire JSON 中唯一 `content` 槽位，并在投影前后各执行真实 route preparation，验证 provider route/protocol、call/result 配对、顺序、输入、witness 和除选中正文外的全部 wire 值未变。list、media、binary、截断页、未知结构、动态指令、provider-executed、失败或运行中结果均保护。

本步没有实施 S07 的旧清理入口退役、外部 DCP 插件识别和迁移诊断，也没有实施 S08–S10 的真实宿主、性能、CI 或测试环境交付。当前切片没有独立可发布测试环境，因此没有部署声明；即使门禁通过，仍只标记为“待父任务审计”。

## 2. 修改范围

- `packages/core/src/session/context-folding/tool-source-ledger.ts` 及模块导出
- `packages/core/src/session/runner/context-folding.ts`
- `packages/core/src/session/runner/llm.ts`
- `packages/core/src/session/runner/to-llm-message.ts`
- `packages/core/src/tool/tool.ts`
- `packages/core/src/tool/application-tools.ts`
- `packages/core/src/tool/registry.ts`
- `packages/core/src/tool/context-folding-builtins.ts`
- 内置 `read`、`grep`、`glob` 的可信指令标记和 host-internal 注册接线
- `packages/core/src/tool/AGENTS.md` 的注册边界说明
- `packages/core/test/session/context-folding-runner.test.ts`
- `packages/core/test/session/session-runner-hotpath.test.ts`
- `packages/core/test/session-runner-tool-registry.test.ts`
- 本证据和实施进度表

## 3. 可证伪验收

### I02：真实 Core runner 闭环

正式测试 `SessionRunnerLLM hot path > folds duplicate built-in read, grep, and glob outputs only in the final outbound request` 从真实 Core runner 依次执行两次 builtin read、grep、glob，经注册、实际结算、来源台账、持久化历史和下一次普通请求进入真实 OpenAI-chat route body conversion。断言三个旧结果均在最终 outbound body 中折叠，三个较晚 witness 保持完整；再次读取会话存储并逐项确认六个原始工具记录的输入、结构化结果、正文、身份及顺序完全不变。

### I13/I14：TextPage 正向与未知结构保护

- `accepts only complete exact TextPage pairs and binds every outer field`
- `rejects list, binary, media, failed, running, dynamic, and unknown-instruction results`
- `rejects captured history after input, body, metadata, order, deletion, or cross-tool ID mutation`
- `projects only conversation requests and leaves disabled, compaction, auxiliary, and unknown purposes untouched`

正向用例使用真实 `new ReadToolFileSystem.TextPage(...)`，验证仅 `content` 改为固定占位，JSON result 类型及全部外层字段不变。负向 fixture 覆盖外层字段/类型/额外字段差异、截断、list、media、binary、未知 shape、动态或未知指令、失败/运行中状态；最终 input、body、metadata、持久化外层字段、顺序、删除或跨工具 ID 碰撞任一变化都返回未投影。

### U23：来源身份与父审计 A01/P1 修复

- `keeps application and ordinary local settlement provenance untrusted and invalidates overridden history`
- `rejects same-name public overrides and restores trusted builtins only for fresh-generation settlements`
- `fails closed for missing provenance and invalidates recorded history after a generation change`
- `does not trust pre-restart history in a fresh provenance ledger`

这些测试经过真实 ToolRegistry materialization/settlement 和真实 Core history conversion/planning，而不是直接伪造 planner 的 `host-builtin` 输入。新增回归先通过内部 capability 注册可信 `grep`，再通过公开 `ToolRegistry.register` 安装同名且携带 safe marker 的 override：两次实际 settlement 都记录为 `custom`，最终 history 得到 0 个 replacement 和两个 `untrusted-source`。关闭 override 后，override 前捕获 turn 的 settlement 因旧 generation 被拒绝；只有新 materialization 的两次真实 builtin settlement 产生一个合法 replacement。

父审计冻结失败提交为 `c79f266cf828af5d0b1eef7f2cddec08350b02bf`。对父任务提供的原始复现脚本 `/private/tmp/context-folding-s06-parent-provenance.ts` 不作修改直接重跑，修复前证据为两个 `host-builtin/none` 且 `replacements=1`；修复后为两个 `custom/none`、`replacements=0`，两个 exclusion 均为 `untrusted-source`。

## 4. 实际门禁

最终有效证据全部使用仓库锁定的 `bun@1.3.14`。package 测试均在对应 package 内执行，未从仓库根运行 `bun test`。

| 检查                       | 结果                                                                        | 原始结果路径                                             |
| -------------------------- | --------------------------------------------------------------------------- | -------------------------------------------------------- |
| 父审计原始复现脚本         | 两次 settlement 均为 `custom/none`；0 replacement；两个 `untrusted-source`  | `/private/tmp/opencode-dag-s06-parent-probe-fixed.log`   |
| Core 聚焦及直接影响测试    | 54 pass、0 fail、7,344 assertions                                           | `/private/tmp/opencode-dag-s06-core-focused-fix.log`     |
| Core 全量测试              | 1,295 pass、0 fail、11,143 assertions、158 files                            | `/private/tmp/opencode-dag-s06-core-full-fix.log`        |
| OpenCode S05 兼容回归      | 43 pass、0 fail、134 assertions                                             | `/private/tmp/opencode-dag-s06-opencode-context-fix.log` |
| Core package typecheck     | 成功                                                                        | `/private/tmp/opencode-dag-s06-core-typecheck-fix.log`   |
| 根 typecheck               | 29/29 tasks successful；24 cached                                           | `/private/tmp/opencode-dag-s06-root-typecheck-fix.log`   |
| 根 lint                    | 0 errors；4,843 warnings，低于 4,850 ratchet                                | `/private/tmp/opencode-dag-s06-lint-fix.log`             |
| SpecGit Issue 只读 inspect | Issue #605 adopted；`writes:false`                                          | `/private/tmp/opencode-dag-s06-specgit-issue.log`        |
| SpecGit PR 只读 status     | PR #606 与当前 S06 source identity 不同，预期 `identity_mismatch`，无写操作 | `/private/tmp/opencode-dag-s06-specgit-pr.log`           |

Core 全量测试在受限沙箱内的首次尝试只有环境失败：用户缓存写入、FSEvents 和既有 ripgrep 运行资产被沙箱阻止；允许这些既有测试资源后，同一源码完整重跑得到表中的 1,295/1,295 通过。OpenCode `llm.test.ts` 首次在沙箱内无法绑定随机 localhost 端口；允许测试 fixture 监听后，同一源码重跑 43/43 通过。这些环境失败没有被计作产品通过，也没有通过跳过测试规避。

## 5. 图索引、交付和限制

当前 codebase-memory project `Users-suntao-.codex-worktrees-cd4e-opencode-dag` 的最终验证 generation 为 `2026-09-16T08:23:32Z`。对本步 11 个生产源码和 3 个测试路径执行 `check_index_coverage`，全部为 `metadata_match`、`no_recorded_issue`；测试、直接源码读取和实际 route body 捕获仍是语义结论的主要依据。coverage clean 只表示索引没有记录缺口，不是完整性的独立证明。

`specgit 2.0.1` 对 Issue #605 的只读 inspect 成功；PR #606 仍属于旧 source，当前分支只读 status 返回预期 `identity_mismatch`。本子任务没有创建、更新、ready、合并或发布 PR，也没有修改 Issue 状态。

最终 wire 正向捕获使用本地可重复的 OpenAI-chat route conversion 和模拟 provider stream，没有调用外部模型或网络供应商；其他 Core provider locator 均失败关闭，跨 provider 的真实宿主执行仍属于 S08。S07 尚未实施，因此 `knownExternalDcp` 保持 `unknown`，没有声称完成外部插件兼容、旧 prune 退役或完整产品交付。
